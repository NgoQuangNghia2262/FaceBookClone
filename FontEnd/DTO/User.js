class User {
  constructor(obj) {
    (this.Id = obj.Id),
      (this.BirthDay = obj.BirthDay),
      (this.Address = obj.Address),
      (this.Img = obj.Img),
      (this.Name = obj.Name),
      (this.Phone = obj.Phone);
  }
}
